name: Install sops
description: Install sops using mise (version defined in mise.toml)
runs:
  using: composite
  steps:
    - uses: jdx/mise-action@e6a8b3978addb5a52f2b4cd9d91eafa7f0ab959d # v4.2.0
      with:
        install: false
    - shell: bash
      run: mise install sops

name: Main CI

on:
  merge_group:
    types:
      - checks_requested
  pull_request:
    branches:
      - "main"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  REVIEWDOG_GITHUB_API_TOKEN: ${{ github.token }}

jobs:
  all-status-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs:
      - go-version-sync-check
      - ansible-lint
      - check-symlinks
      # - dockerfile-lint
      - yaml-lint
      - compose-config-schema-check
      - markdown-lint
      - shell-lint
      - action-lint
      - dotenv-lint
      - kics
      - sops-secrets-check
      - renovate-deps-comment
      - gitleaks
      - trufflehog
      - secret-strength-linter-test
      - secret-strength-linter
    if: ${{ always() }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Check all-status-check
        run: |
          diff \
            <(yq ".jobs | del(.all-status-check) | keys.[]" .github/workflows/ci.yml) \
            <(yq ".jobs.all-status-check.needs.[]" .github/workflows/ci.yml)
      - name: Fail if any needed job failed
        env:
          JOBS: ${{ toJson(needs) }}
        run: |
          for result in $(jq -r '.[].result' <<<"$JOBS"); do
            if [[ ! "$result" =~ ^(success|skipped)$ ]]; then
              exit 1
            fi
          done

  go-version-sync-check:
    name: Go version sync check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Compare all go.mod and mise.toml Go versions
        run: |
          set -euo pipefail

          mise_go_version="$(sed -nE 's/^go[[:space:]]*=[[:space:]]*"([^"]+)".*/\1/p' mise.toml | head -n1)"

          if [[ -z "${mise_go_version}" ]]; then
            echo "Failed to read Go version from mise.toml"
            exit 1
          fi

          mismatch=0
          found=0

          while IFS= read -r go_mod_file; do
            found=1
            go_mod_version="$(awk '/^go / {print $2; exit}' "${go_mod_file}")"

            if [[ -z "${go_mod_version}" ]]; then
              echo "Failed to read Go version from ${go_mod_file}"
              mismatch=1
              continue
            fi

            if [[ "${go_mod_version}" != "${mise_go_version}" ]]; then
              echo "Go version mismatch detected:"
              echo "  file:      ${go_mod_file}"
              echo "  go.mod:    ${go_mod_version}"
              echo "  mise.toml: ${mise_go_version}"
              mismatch=1
            fi
          done < <(find . -type f -name "go.mod" | sort)

          if [[ "${found}" -eq 0 ]]; then
            echo "No go.mod files found"
            exit 1
          fi

          if [[ "${mismatch}" -ne 0 ]]; then
            exit 1
          fi

          echo "All go.mod files are in sync with mise.toml: ${mise_go_version}"

  ansible-lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
      - name: Install ansible and ansible-lint
        run: uv sync --project tools/ansible
      - name: Run ansible-galaxy
        run: uv run --project tools/ansible -- bash -c "cd ansible && ansible-galaxy install -r requirements.yml"
      - name: Run ansible-lint
        run: uv run --project tools/ansible -- bash -c "cd ansible && ansible-lint"

  check-symlinks:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Find symlinks
        run: |
          # CLAUDE.md is an intentional symlink to AGENTS.md and is excluded.
          if [ -n "$(find . -type l -not -path './CLAUDE.md')" ]; then
            echo "Error: Found symbolic links in the repository"
            find . -type l -not -path './CLAUDE.md'
            exit 1
          fi

  # dockerfile-lint:
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 10
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Dockerfile lint
  #       uses: reviewdog/action-hadolint@v1.46.0
  #       with:
  #         level: warning

  yaml-lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: reviewdog/action-yamllint@b5f7217d8c815ae374d1d55840d5e569d82f01f0 # v1.22.0
        with:
          github_token: ${{ github.token }}
          level: warning
          yamllint_flags: "-c .yamllint ."

  compose-config-schema-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version-file: tools/cmt/go.mod
          cache-dependency-path: tools/cmt/go.sum
      - name: Install check-jsonschema
        run: python -m pip install --upgrade check-jsonschema
      - name: Generate cmt schemas
        working-directory: tools/cmt
        run: |
          set -euo pipefail
          mkdir -p /tmp/cmt-schemas
          go run . schema cmt > /tmp/cmt-schemas/cmt.schema.json
          go run . schema host > /tmp/cmt-schemas/host.schema.json
      - name: Validate compose config YAML files
        run: |
          set -euo pipefail
          python -m check_jsonschema --schemafile /tmp/cmt-schemas/cmt.schema.json compose/config.yml
          python -m check_jsonschema --schemafile /tmp/cmt-schemas/host.schema.json compose/hosts/*/host.yml

  markdown-lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Install tools
        run: npm ci
        working-directory: .github/tools
      - name: Run markdownlint
        run: .github/tools/node_modules/.bin/markdownlint . || true

  shell-lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: reviewdog/action-shellcheck@4c07458293ac342d477251099501a718ae5ef86e # v1.32.0
        with:
          github_token: ${{ github.token }}
          level: warning
      - uses: jdx/mise-action@e6a8b3978addb5a52f2b4cd9d91eafa7f0ab959d # v4.2.0
        with:
          install: false
      - name: Install shfmt
        run: mise install shfmt
      - name: Run shfmt
        run: shfmt -i 2 -ci -w .
      - uses: reviewdog/action-suggester@2558ba17e65a9039e73764a73009fc05fef28a46 # v1.24.3
        with:
          tool_name: shfmt
          github_token: ${{ github.token }}
          level: warning

  action-lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: reviewdog/action-actionlint@6fb7acc99f4a1008869fa8a0f09cfca740837d9d # v1.72.0
        with:
          github_token: ${{ github.token }}
          level: warning
          fail_level: any

  dotenv-lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: dotenv-linter/action-dotenv-linter@afde61cfda2ecffe7bea35837b6f20b956c88689 # v3.0.0
        with:
          github_token: ${{ github.token }}

  kics:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    continue-on-error: true
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Run KICS via Docker (Makefile target)
        run: make kics

  sops-secrets-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Run sops secrets check
        run: make sops-ci

  renovate-deps-comment:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: ${{ github.event_name == 'pull_request' && startsWith(github.head_ref, 'renovate/') }}
    continue-on-error: true
    steps:
      - uses: shiron-dev/actions/renovate-deps-comment@8780bd20d1ef046844aa1a1c2e9d96fefaa1384b # v1.7.0

  gitleaks:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
      - name: Run gitleaks
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          if [ -z "${BASE_SHA}" ] || [ "${BASE_SHA}" = "0000000000000000000000000000000000000000" ]; then
            log_opts="${HEAD_SHA}^..${HEAD_SHA}"
          else
            log_opts="${BASE_SHA}..${HEAD_SHA}"
          fi

          docker run --rm \
            -v "$PWD:/repo" \
            ghcr.io/gitleaks/gitleaks:v8.30.1 \
            git --verbose --log-opts="${log_opts}" /repo

  trufflehog:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
      - name: Run TruffleHog
        uses: trufflesecurity/trufflehog@30d5bb91af1a771378349dbbb0c82129392acf70 # v3.95.6
        with:
          path: ./
          base: ${{ github.event.pull_request.base.sha || github.event.before }}
          head: ${{ github.event.pull_request.head.sha || github.sha }}

  secret-strength-linter-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
      - name: Detect linter changes
        id: detect
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.event.merge_group.head_sha || github.sha }}
        run: |
          set -euo pipefail
          # Without a usable base (e.g. workflow_dispatch) run unconditionally.
          if [ -z "${BASE_SHA}" ] || [ "${BASE_SHA}" = "0000000000000000000000000000000000000000" ]; then
            echo "No base ref; running tests"
            echo "skip=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          CHANGED=$(git diff --diff-filter=ACMR --name-only "${BASE_SHA}..${HEAD_SHA}" -- tools/secret-strength-linter)
          if [ -z "${CHANGED}" ]; then
            echo "No changes under tools/secret-strength-linter, skipping"
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "Changed files:"
            echo "${CHANGED}"
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi
      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        if: steps.detect.outputs.skip == 'false'
      - name: Install dependencies
        if: steps.detect.outputs.skip == 'false'
        run: uv sync --project tools/secret-strength-linter
      - name: Run pytest
        if: steps.detect.outputs.skip == 'false'
        run: uv run --project tools/secret-strength-linter pytest tools/secret-strength-linter

  secret-strength-linter:
    # SOPS strength check needs a base..head diff, so only run on pull requests.
    if: ${{ github.event_name == 'pull_request' }}
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/secret-strength-linter.yml
    with:
      base_sha: ${{ github.event.pull_request.base.sha }}
      head_sha: ${{ github.event.pull_request.head.sha }}

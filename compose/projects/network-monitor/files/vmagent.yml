global:
  scrape_interval: 15s
  external_labels:
    probe_server: 'home-ep'
scrape_configs:
  - job_name: 'vmagent'
    scrape_interval: 5s
    scrape_timeout: 5s
    static_configs:
      - targets: ['localhost:8429']
  - job_name: 'node'
    scrape_interval: 15s
    scrape_timeout: 10s
    static_configs:
      - targets:
          - host.docker.internal:9100
        labels:
          host: home-ep
          probe_server: home-ep
  # home-ep 上の Docker Compose コンテナの状態を docker-compose-exporter から
  # 取得する。container/state/health/restart count を Prometheus 形式で公開する。
  # probe_server ラベルは global.external_labels (home-ep) が自動付与するため
  # ここでは付けない (arm-srv 側の docker_compose ジョブと同じ流儀)。
  - job_name: 'docker_compose'
    scrape_interval: 15s
    scrape_timeout: 10s
    static_configs:
      - targets:
          - home_ep_docker_compose_exporter:9487
  # 自前の icmp-ping-exporter が 10 分ごとに 5 発 ping を打って状態を保持する。
  # vmagent はその状態を 30s ごとに取りに行くだけ (実際の値は 10 分ごとに更新)。
  - job_name: 'home_ep_icmp'
    scrape_interval: 30s
    scrape_timeout: 10s
    static_configs:
      - targets:
          - home_ep_icmp_ping_exporter:9799
        labels:
          probe_server: home-ep
          target_server: google-public-dns
          check: icmp
  - job_name: 'home_ep_cloudflare_speedtest'
    scrape_interval: 30s
    scrape_timeout: 10s
    static_configs:
      - targets:
          - home_ep_cloudflare_speedtest_exporter:9798
        labels:
          probe_server: home-ep
          target_server: speed.cloudflare.com

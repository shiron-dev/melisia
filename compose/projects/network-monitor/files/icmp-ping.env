# icmp-ping-exporter の調整値。値を変えたいときはここを編集して cmt apply する
# だけ (ソース icmp-ping-exporter.py も compose 定義も触らない)。docker compose が
# env_file としてコンテナへ注入する。
HOME_EP_ICMP_TARGET=8.8.8.8
HOME_EP_ICMP_INTERVAL_SECONDS=600
HOME_EP_ICMP_PACKET_COUNT=5
HOME_EP_ICMP_PACKET_TIMEOUT_SECONDS=2

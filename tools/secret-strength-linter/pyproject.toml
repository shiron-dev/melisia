[project]
name = "secret-strength-linter"
version = "0.1.0"
description = "Check strength of secrets in SOPS-encrypted files"
requires-python = ">=3.12"
dependencies = [
    "python-hcl2>=8.1.2",
    "zxcvbn>=4.5.0",
]

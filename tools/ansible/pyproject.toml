[project]
name = "ansible-tools"
version = "0.1.0"
description = "Ansible and ansible-lint for CI and local use"
readme = "README.md"
requires-python = ">=3.14.5"
dependencies = [
    "ansible>=14.0.0",
    "ansible-lint>=26.4.0,<27",
]

[project]
name = "ansible-tools"
version = "0.1.0"
description = "Ansible and ansible-lint for CI and local use"
readme = "README.md"
requires-python = ">=3.14.3"
dependencies = [
    "ansible>=13.4.0",
    "ansible-lint>=26.3.0,<27",
]

- name: Home endpoints
  hosts: home_ep
  roles:
    - raspberrypi
    # TODO: Re-enable Cloudflare roles after the temporary Cloudflare changes are restored.
    # - cloudflare_mesh
    # - cloudflare_mesh_dns_updater
    # - cloudflare_access_infrastructure_ssh
    - docker

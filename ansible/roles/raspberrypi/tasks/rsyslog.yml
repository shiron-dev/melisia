- name: Apt-get update
  become: true
  ansible.builtin.apt:
    update_cache: true

- name: Install rsyslog
  become: true
  ansible.builtin.apt:
    name: rsyslog
  notify: Reboot

- name: Enable rsyslog
  become: true
  ansible.builtin.service:
    name: rsyslog
    enabled: true
